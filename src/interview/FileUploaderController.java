package interview;

import java.io.BufferedInputStream;
import java.io.File;

//@RestController("/upload")
public class FileUploaderController {

    /*@AutoWire
    public fileUploaderService;

    @Path("/file")
    @PostMapping
    public ResponseEntity uploadFile(RequestBody body, @RequestParam String fileName) {
        File file = new File("/tmp/"+fileName);
        BufferedInputStream is = new BufferedInputStream(body.getInputStream());
        is.;

    }*/

}
